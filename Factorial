#include <iostream>
#include <string>
using namespace std;
int main()
{
	cout << "Enter a number for factorial\n";
	int x, fact = 1;
	cin >> x;
	for (int y = x; y > 0; y--)
	{
		fact = fact * y;
	}
	cout << "the factorial is : " << fact;
}
